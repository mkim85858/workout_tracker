# Minimal prototype config (BLE keys are placeholders for later)
ble:
  adapter: "hci0"
  # Use exactly one of the following target selectors later:
  target_address: "BC:93:07:7C:38:ED"
  target_name: "WorkoutApp"  # optional, for scanning prototype
  service_uuid: "6E400001-B5A3-F393-E0A9-E50E24DCCA9E"           # fill when we add BLE
  char_uuid_tx: "6E400002-B5A3-F393-E0A9-E50E24DCCA9E"           # fill when we add BLE
  write_without_response: true

  scan_timeout_ms: 5000
  connect_timeout_ms: 8000
  retry_backoff_ms: 1000
  burst_coalesce_ms: 250
